import { Inter } from "next/font/google";

const inter = Inter({ subsets: ["latin"] });

const Home = () => {
  return (
    <div>
      <h1>welcome to information hub</h1>
    </div>
  );
};

export default Home;
